/**
 * @author {Mr.Martin}
 * @description {封装回调函数callback}
 */

(function() {
  /**
   * 应用场景(例子):
   * _.map(obj, interatee, context) {}
   * obj是数组
   * interatee是处理函数
   * context是执行上下文(也就是this指向, 其实执行上下文还包括作用域链和活动对象等等)
   */
 
 })()